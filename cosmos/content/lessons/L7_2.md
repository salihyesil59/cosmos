---
id: L7.2
title: "From Data to a Number: Likelihood, χ² and 5σ"
summary: How a table of measurements becomes "Ωm = 0.33 ± 0.02". The likelihood, the chi-squared, what a sigma really means, and why cosmologists let a random walker do the work.
minutes: 20
prerequisites: [L7.1, L3.3]
simulators: [S19, S16]
objectives:
  - How a model plus data gives a likelihood
  - Why χ² is the likelihood in disguise, and what χ²/dof tells you
  - What "5σ" means, and what it does not
  - Why parameters are estimated with a random walk (MCMC) rather than a formula
---
## The question behind every number

Papers end with statements like $\Omega_{\mathrm{m}} = 0.334 \pm 0.018$. Where
does such a line come from? Always from the same three ingredients:

1. a **model** that predicts what should be measured, for each set of parameters;
2. a **likelihood** that scores how well a prediction matches the data;
3. a way to **explore** the parameters and report the range that survives.

## Step 1: the likelihood

Suppose each measurement $d_i$ has a Gaussian uncertainty $\sigma_i$ and the
model predicts $m_i(\theta)$ for parameters $\theta$. The probability of getting
the data you actually got is

$$\mathcal{L}(\theta) = \prod_i \frac{1}{\sqrt{2\pi}\sigma_i}
\exp\!\left[-\frac{(d_i - m_i(\theta))^2}{2\sigma_i^2}\right]$$

Multiplying thousands of small numbers is numerically hopeless, so everyone works
with its logarithm, and the constants in front do not depend on $\theta$:

$$-2\ln\mathcal{L}(\theta) = \sum_i \frac{(d_i - m_i(\theta))^2}{\sigma_i^2}
+ \text{constant} \;\equiv\; \chi^2(\theta) + \text{constant}$$

:::key χ² is not a separate idea
For Gaussian errors, **minimising χ² and maximising the likelihood are the same
operation**. Every point contributes the square of its miss, measured in units of
its own error bar. A point that misses by 2σ costs four times as much as one that
misses by 1σ.
:::

## Step 2: reading χ²

The minimum value of χ² says how good the best fit is. With $N$ data points and
$k$ fitted parameters there are $N - k$ **degrees of freedom**, and each one
should contribute about 1:

$$\chi^2/\mathrm{dof} \approx 1$$

- Much larger than 1: the model is wrong, or the error bars are too small.
- Much smaller than 1: the error bars are too large (or correlated errors have
  been treated as independent — the usual reason in cosmology).

:::example What the app gets
Fitting the real Pantheon+ supernovae in the simulators here gives χ²/dof ≈ 0.44.
That is not a miraculous fit: the bundled errors are the diagonal ones only, and
the published analysis uses a full covariance matrix that ties nearby supernovae
together. The best-fit parameters survive this simplification; the error bars are
the part you should not quote.
:::

## Step 3: error bars from the curvature

Move one parameter away from its best value and χ² rises. How far you may move
before the fit becomes unacceptable **is** the error bar:

$$\Delta\chi^2 = 1 \;\Rightarrow\; 1\sigma, \qquad
\Delta\chi^2 = 4 \;\Rightarrow\; 2\sigma, \qquad
\Delta\chi^2 = 25 \;\Rightarrow\; 5\sigma$$

{{figure:chi2_parabola}}

A sharp, narrow parabola means a precise measurement; a shallow one means the
data barely care about that parameter. For a contour in **two** parameters the
thresholds are larger (Δχ² = 2.30 and 6.17 for 68% and 95%), because two things
are being allowed to vary at once.

:::warning What 5σ does and does not mean
"5σ" says: *if there were no effect, data this extreme would appear by chance
about once in 3.5 million times.* It is a statement about randomness only. It
says nothing about a miscalibrated telescope, an unnoticed selection effect or a
wrong model — and those are what usually go wrong. Several 5σ results in the
history of physics were systematics, not discoveries.
:::

## Step 4: exploring the parameters

With two parameters you can simply compute χ² on a grid, as simulator S16 does.
With six — the standard ΛCDM set — a grid of 100 points per axis needs 10¹²
evaluations. Nobody does that. Instead a **random walker** explores the parameter
space: this is [[mcmc|Markov chain Monte Carlo]].

The Metropolis rule is three lines:

1. From the current parameters, propose a nearby set.
2. Compute the likelihood there. If it is better, move.
3. If it is worse by a factor $r$, move anyway with probability $r$; otherwise
   stay where you are and write the current point down again.

{{figure:mcmc_walk}}

Run this long enough and the density of recorded points **is** the posterior
distribution. The mean of the cloud is the measurement, its width is the error
bar, and its tilt is the correlation between parameters.

:::note The three numbers that tell you a chain is healthy
- **Acceptance rate** — around 25% for a few parameters. Near 0% or 100% means
  the proposal step is the wrong size.
- **Burn-in** — the walker starts wherever you put it; those first steps are
  discarded.
- **R̂** — start several walkers far apart. If they end up describing the same
  distribution, R̂ ≈ 1. Above about 1.01, they have not converged.
:::

:::try S19 Run the chain on the real supernovae, then set the proposal step to 0.005 and to 0.4 and watch the acceptance rate, the shape of the cloud and the effective sample size.

## Priors, briefly

A likelihood tells you how well parameters fit the data; a **prior** says what
you believed before. Multiplying them gives the **posterior**, which is what a
chain samples. In cosmology most priors are deliberately dull — "Ωm is between 0
and 1" — but they matter whenever the data are weak, and honest papers say what
they used.

## Summary

- A model plus Gaussian errors gives a likelihood; $-2\ln\mathcal{L}$ is χ².
- χ²/dof near 1 means the model and the error bars are both plausible.
- Error bars come from how fast χ² rises: Δχ² = 1, 4, 25 for 1σ, 2σ, 5σ.
- A sigma counts randomness only; systematics are a separate, harder argument.
- MCMC turns parameter estimation into a walk, and the cloud of visited points is
  the answer, error bars and correlations included.
